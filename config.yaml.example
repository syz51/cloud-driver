# Cloud Driver Server Configuration
# Copy this file to config.yml and modify as needed

server:
  host: "0.0.0.0"  # Server bind address (0.0.0.0 for all interfaces)
  port: 8081       # Server port

